name: 'pr_sig_check'
description: 'Checks your PRs for Signatures'
inputs:
  #orgcheck:
  #  description: "Limit 'Acceptable' Organizations to the following list (comma seperated)."
  #  required: false
  #  default: ""
  domaincheck:
    description: "Limit 'Acceptable' committer emails to the following list (comma seperated)."
    required: false
    default: ""
  verbose:
    description: "Verbosity as a number with higher numbers representing more verbosity."
    required: false
    default: 0
runs:
  using: 'docker'
  image: 'Dockerfile'